"""OptiX SDK header-only library."""

load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "optix",
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)
